# URL base da API do backend (sem barra no final).
# Backend em produção (Render):
VITE_API_URL=https://internship-attendance-program.onrender.com
#
# Para usar o backend local em desenvolvimento, use:
# VITE_API_URL=http://localhost:3000
#
# Se não definir, o frontend em dev usará o proxy do Vite (backend na porta 3000).
