# URL base da API do backend (sem barra no final).
# Em desenvolvimento, com o backend em http://localhost:3000, use:
# VITE_API_URL=http://localhost:3000
#
# Se não definir, o frontend em dev usará o proxy do Vite para /frequencia, /relatorio e /estagiario
# (backend na porta 3000; dados vêm de backend/data/frequencias.json e estagiarios.json).
VITE_API_URL=http://localhost:3000
