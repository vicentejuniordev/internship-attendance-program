# URL base da API do backend (sem barra no final).
# Em desenvolvimento, se o backend estiver em http://localhost:3000, use:
# VITE_API_URL=http://localhost:3000
#
# Se não definir, o frontend em dev usará o proxy do Vite para /frequencia
# (configure o backend na porta 3000 para o proxy funcionar).
VITE_API_URL=http://localhost:3000
